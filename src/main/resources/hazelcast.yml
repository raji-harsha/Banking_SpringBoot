hazelcast:
  network:
    join:
      auto-detection: 
        enabled: true
      multicast:
        enabled: true
        #multicast-port:54327 # default port no on which hazelcast member listens and sends discovery messages
